---
import "../styles/global.css";

const text = "WHERE CODE MEETS CRAFT — ";
const repeated = text.repeat(8);
---

<div class="overflow-hidden border-y border-cream/20 py-3">
  <div class="flex whitespace-nowrap animate-marquee">
    <span class="font-mono text-xs tracking-[0.25em] text-cream/70 pr-0">
      {repeated}
    </span>
    <span class="font-mono text-xs tracking-[0.25em] text-cream/70 pr-0">
      {repeated}
    </span>
  </div>
</div>
